# Act configuration file
# This file configures act for local GitHub Actions testing

# Use specific container architecture for Apple Silicon
--container-architecture linux/amd64

# Bind working directory to container (faster than copying)
--bind

# Use default actor (can be overridden via CLI)
--actor nektos/act-bot

# Enable verbose output for debugging
--verbose
