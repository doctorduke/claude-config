# GitHub Actions Runner Token Refresh Configuration
# Copy to /etc/github-runner/token-refresh.env and customize

# GitHub organization name (REQUIRED)
RUNNER_ORG=your-org-name

# Runner configuration
RUNNER_NAME=my-runner-01
RUNNER_URL=https://github.com/your-org-name
RUNNER_DIR=/home/runner/actions-runner

# Token refresh settings
# Refresh token this many seconds before it expires (default: 300 = 5 minutes)
REFRESH_THRESHOLD=300

# Check token expiration every N seconds (default: 60)
CHECK_INTERVAL=60

# Retry configuration
MAX_RETRY_ATTEMPTS=3
RETRY_BACKOFF_SECONDS=30

# Logging and metrics
LOG_FILE=/var/log/github-runner-token-refresh.log
METRICS_FILE=/var/tmp/runner-token-metrics.json

# Log level (optional, uncomment to enable)
# CURRENT_LOG_LEVEL=1  # 0=DEBUG, 1=INFO, 2=WARN, 3=ERROR
